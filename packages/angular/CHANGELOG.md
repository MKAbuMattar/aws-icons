# @aws-icons/angular

## 2.0.0

### Major Changes

- 0de8164: Three more framework packages:

  - `@aws-icons/angular` — standalone `awsIcon` directive (partial-Ivy,
    ng-packagr build) + tree-shakeable icon data
  - `@aws-icons/lit` — `<aws-icon>` LitElement and `iconTemplate`
    helper + tree-shakeable icon data
  - `@aws-icons/htmx` — server handler and `iconHtml` helper serving
    icon SVG fragments
